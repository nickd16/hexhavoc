class Effect {
    constructor(){
        this.steps = ["","",""]
        this.player;
    }
    async parseStep(step=""){
        tokens = step.split(" ");
        if (tokens[0] == "input"){
            
        } ; 
    }

    async run(){
        for (let i = 0; i < this.steps.length(); i++){
            
        }
    }
}

export {Effect}